# Time-Labeled Device Interface (TLDI)

TLDI is how Android becomes a time-aware agent node.

Each device event becomes:

- time-labeled (blockId, segmentId)
- network-labeled (channel, priority)
- fingerprinted if hardware-related
- securely routable via Duo Bluetooth
